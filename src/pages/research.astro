---
import Layout from '~/layouts/PageLayout.astro';
import BlogList from '~/components/blog/List.astro';
import Headline from '~/components/blog/Headline.astro';
import BlogHighlightedPosts from '~/components/widgets/BlogHighlightedPosts.astro';
import { fetchPosts } from '~/utils/blog';

const posts = await fetchPosts();
const metadata = {
  title: 'Research & Insights — NREC',
};
---

<Layout metadata={metadata}>
  <BlogHighlightedPosts
    title="Premium Insights"
    information="Our research combines analytical rigor with real estate market trends. Discover the latest reports produced by NREC's top talent."
  />

  <section class="px-6 py-12 sm:py-16 lg:py-20 mx-auto max-w-7xl">
    <Headline
      subtitle="Explore our complete archive of market analysis, real estate trends, and opinion articles."
    >
      Library & Reports
    </Headline>
    
    {posts.length > 0 ? (
      <BlogList posts={posts} />
    ) : (
      <p class="text-center text-muted">New reports coming soon. Stay tuned!</p>
    )}
  </section>
</Layout>
